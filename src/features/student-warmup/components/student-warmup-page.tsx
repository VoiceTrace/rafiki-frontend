"use client";
import { useState } from "react";
import { CheckCircle2, ChevronRight, MessageCircleQuestion, Sparkles } from "lucide-react";
import { useTranslations } from "next-intl";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
export function StudentWarmupPage(){const t=useTranslations("studentWarmup");const [choice,setChoice]=useState<number|null>(null);const [submitted,setSubmitted]=useState(false);const options=[t("options.one"),t("options.two"),t("options.three")];return <div className="mx-auto flex w-full max-w-[760px] flex-col gap-4 pb-20"><header><div className="flex items-center gap-2 text-assistant-foreground"><Sparkles className="size-5"/><span className="text-sm font-semibold">{t("eyebrow")}</span></div><h1 className="mt-1 font-heading text-page font-bold">{t("title")}</h1><p className="mt-1 text-sm text-muted-foreground">{t("subtitle")}</p></header><Card className="border-assistant bg-assistant/30 shadow-surface"><CardHeader><CardTitle>{t("questionTitle")}</CardTitle></CardHeader><CardContent><p className="rounded-xl bg-card p-4 text-base leading-7">{t("question")}</p><div className="mt-4 grid gap-3">{options.map((option,index)=><button type="button" key={option} onClick={()=>{setChoice(index);setSubmitted(false)}} className={`rounded-xl border px-4 py-3 text-start text-sm font-medium ${choice===index?"border-primary bg-secondary":"border-border bg-card"}`}>{option}</button>)}</div>{submitted&&<div className="mt-4 rounded-xl border border-success bg-success p-4 text-sm"><CheckCircle2 className="me-2 inline size-5 text-success-foreground"/>{t("feedback")}</div>}<div className="mt-5 flex flex-col gap-2 sm:flex-row"><Button type="button" variant="outline" className="sm:flex-1"><MessageCircleQuestion/>{t("ask")}</Button><Button type="button" className="sm:flex-1" disabled={choice===null} onClick={()=>setSubmitted(true)}>{t("submit")}<ChevronRight className="rtl:-scale-x-100"/></Button></div></CardContent></Card></div>}
