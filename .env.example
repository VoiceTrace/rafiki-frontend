# Generate with: npx auth secret
AUTH_SECRET=

# Backend origin only; do not include a trailing slash.
AUTH_API_URL=http://localhost:8080

# Development only. When AUTH_API_URL is omitted, mock auth is enabled by default.
# Set this to false to exercise the unavailable-backend state locally.
AUTH_USE_MOCK_BACKEND=true

